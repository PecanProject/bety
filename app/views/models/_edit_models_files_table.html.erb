<table>
  <tr>
    <th></th>
    <th></th>
    <th>Name</th>
  </tr>
    <% @files.each do |file| %>
    <tr>
      <td>
      <% if @model.files.include?(file) %>
        <%= link_to "X", { :controller => :models, :action => :edit_models_files, :id => @model.id, :file => file, :search => @searchparam }, :confirm => 'Are you sure?', :remote => true,
                          :html => { :style => "text-decoration: none" } %>


      <% else %>
        <%= link_to "+", { :controller => :models, :action => :edit_models_files, :id => @model.id, :file => file, :search => @searchparam }, :confirm => 'Are you sure?', :remote => true,
                          :html => { :style => "text-decoration: none" } %>


      <% end %>
      </td>
      <td><%= link_to "edit", edit_dbfile_path(file) %></td>
      <td><%= link_to file, dbfile_path(file) %></td>
      </tr>
    <% end %>
</table>

<%= will_paginate @files, :renderer => 'WillPaginate::ActionView::RemoteLinkRenderer' , :params => {:action => :edit_models_files, :id => @model.id, :search => @searchparam}, :remote => { :loading => "Element.show('spinner')", :complete => "Element.hide('spinner')" } %>

