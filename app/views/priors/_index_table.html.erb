<table class="pretty">
  <tr>
    <th><%= sortable "citations.author", "Citation" -%></th>
    <th><%= sortable "variables.name", "Variable" -%></th>
    <th><%= sortable "priors.phylogeny", "Phylogeny" -%></th>
    <th><%= sortable "priors.distn", "Distribution" -%></th>
    <th><%= sortable "priors.parama", "Parameter a" -%></th>
    <th><%= sortable "priors.paramb", "Parameter b" -%></th>
    <th><%= sortable "priors.n", "N" -%></th>
    <th><%= sortable "priors.notes", "Notes" -%></th>
  </tr>

<% @priors.each do |prior| %>
  <tr<%= " style='background-color:yellow'" if @search and !@search[/[^\d]/] and prior.id == @search.to_i%>>
    <td><%= link_to_if prior.citation, prior.citation, prior.citation %></td>
    <td><%= link_to_if prior.variable, prior.variable, prior.variable %></td>
    <td><%= prior.phylogeny %></td>
    <td><%= prior.distn %></td>
    <td><%= prior.parama %></td>
    <td><%= prior.paramb %></td>
    <td><%= prior.n %></td>
    <td><%= prior.notes %></td>
    <td><%= link_to 'Show', prior %></td>
    <td><%= link_to 'Edit', edit_prior_path(prior) %></td>
    <td><%= link_to 'Destroy', prior, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<%= will_paginate @priors %>

