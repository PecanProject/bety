<% @pfts.each do |pft| %>
    <tr>
    <td>
  <% if @prior.pfts.include?(pft) %>
    <%= link_to "X", { :controller => :priors, :action => :rem_pfts_priors, :id => @prior.id, :pft => pft }, :remote => true %></td>
  <% else %>
    <%= link_to "+", { :controller => :priors, :action => :add_pfts_priors, :id => @prior.id, :pft => pft }, :remote => true %></td>
  <% end %>
    <td><%= link_to "edit", edit_pft_path(pft.id) %></td>
    <td><%= pft.name %></td>
    <td><%= pft.definition %></td>
    </tr>
<% end %>
