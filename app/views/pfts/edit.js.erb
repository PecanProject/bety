/* Restores the priors list as it looked before doing any searches */

jQuery('tbody').html(
    "<%= j render(partial: 'priors_tbody') %>"
);

jQuery('h3').html("Existing Priors Relationships");

jQuery('#search_priors').val('');
