jQuery('table#priors tbody').html(
    "<%= j render(partial: 'priors_tbody') %>"
);

jQuery('table#priors caption').html(
    "All Search Matches" +
        ' <%= link_to "Show only related priors",
              { controller: :pfts, action: :edit, remote: true } %>'
);
