    <h2>Results in: <%= @location %></h2>
    <table>
      <tr>
        <th>Species</th>
        <th>Yield</th>
      </tr>
      <% @results.each do |result| %>
      <tr>
        <td><%= result.species %></td>
        <td><%= result["avg(yield)"] %></td>
      </tr>
      <% end %>
    </table>

