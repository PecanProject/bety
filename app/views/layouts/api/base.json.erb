<%=
  require 'yajl'
  raw({
        metadata: {
          URI: request.url,
          timestamp: DateTime.now
        },
        error: @error,
        data: Yajl::Parser.parse(yield)
      }.to_json)
%>
