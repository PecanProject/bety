<%=
  require 'yajl'
  raw(Yajl::Encoder.new(pretty: true).encode({
        metadata: {
          URI: request.url,
          timestamp: DateTime.now
        },
        error: @error,
        data: Yajl::Parser.parse(yield)
      }))
%>
