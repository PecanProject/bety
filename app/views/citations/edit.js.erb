/* Restores the sites list to show only linked sites */

jQuery('table#sites tbody').html(
    "<%= j render(partial: 'sites_tbody') %>"
);

jQuery('table#sites caption').html("Existing Sites Relationships" +
                                    ' <%= link_to "Update",
                                          { controller: :citations, action: :edit, remote: true },
                                          { style: "display: none" } %>');

jQuery('#search_sites').val('').focus();
