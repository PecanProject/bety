jQuery('table#sites tbody').html(
    "<%= j render(partial: 'sites_tbody') %>"
);

jQuery('h3').html(
    "All Search Matches" +
        ' <%= link_to "Show only related sites", { controller: :citations, action: :edit, remote: true } %>'
    );
